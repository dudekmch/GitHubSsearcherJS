import React from "react";
import styled from "styled-components";

const StyledPageTitle = styled.h3`
color: rgba(255,255,255,0.75);
flex: 1;
`

const PageTitle = () => (
    <StyledPageTitle>GitHub Searcher</StyledPageTitle>
)

export default PageTitle